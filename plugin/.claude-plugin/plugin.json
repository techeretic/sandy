{
  "name": "Sandy",
  "description": "SANDBOXable AI assistant. The host LLM does the reasoning; Sandy executes — gathering data from declared internal services via MCP servers and writing provenance-tracked reports, all confined to your sandbox (no egress outside declared endpoints, except the one-shot, human-confirmed, audited `sandy import` fetch).",
  "version": "0.2.0",
  "author": { "name": "Sandy" },
  "mcpServers": {
    "sandy": {
      "type": "stdio",
      "command": "node",
      "args": ["../dist/plugin/mcp-server.js"],
      "env": { "SANDY_CONFIG": "sandy.json" }
    }
  }
}
