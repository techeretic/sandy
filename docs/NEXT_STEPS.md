# Next Steps — Resume Point

_Last updated 2026-08-29. Forward-looking only: the current state, what's built (so it isn't rebuilt), and what's next. The chronological account of how things got here is `docs/DIARY.md` (append-only) — **do not restate milestones here, point at the DIARY**. Resolved open questions: `docs/DECISIONS.md`. Design decisions: `docs/PHASE2_DESIGN.md` and `docs/MODEL.md`. Commit history: `git log`._

## Status

**v0.1.3 (2026-08-29). Phase 1 and Phase 2 complete; the real bundled model is proven end-to-end. The Q6 approval-gated write path (issue #16) — core _and_ the approval-UX v2 half — is implemented on top, and **issue #14 is now fully closed**: reports render as Markdown (source of truth), HTML, **DOCX, XLSX, or PDF** (`preferences.default_report_format`).**

- **323/323 tests**, typecheck + build green.
- The launch success criterion — "zero network egress outside declared MCP endpoints" — is proven in-process and at the network level in Docker, and the enforcer is proven runtime-agnostic for **both** modes: the same config + request under Docker and Firejail produce byte-identical behavior for plugin (`sandy run`) and standalone (`sandy ask`), in a CI matrix.
- A **real** bundled model (Qwen3-4B-Instruct-2507, Vulkan/GPU, SHA256-pinned) ran the full `sandy ask` loop inside a no-egress sandbox: planned (validated against the policy's legal tool catalog) → MCP tool ran → provenance-tracked report written → model narrated (clearly labeled, SD-06) → usage audited → model process reaped (no orphan). That session also fixed a real integration bug the stub had hidden (`LlamaCppEngine` port discovery had to read **stderr**, where the real `llama-server` logs its listen URL). See `docs/MODEL.md` and `docs/DIARY.md` 2026-08-22 (afternoon).
- The 2026-08-22 full-repo review (`reviews/2026-08-22-full-repo-review.md`) is closed: 7 security findings fixed and released in **v0.1.1** (7 private advisories, all `patched_versions: 0.1.1`), plus 12 follow-up fix PRs (PRs #20–#37) merged. See `docs/DIARY.md` 2026-08-23.

**Nothing is blocking — the tracked deferred scope is fully closed.** Every deferred item (#14 HTML format, #15 templates, #16 write-back core **and** approval-UX v2, #17 real-model leg, #18 memory bound, #19 multi-turn planning) is done; see the "Remaining work" section for the (now-complete) write-ups and the genuinely-optional follow-ups.

## What's built (do not rebuild)

| Module | Path | Covers |
|--------|------|--------|
| Config | `src/config/` | `sandy.json` + `mcp-servers.json` schemas, fail-closed loader, env-ref-only secrets, version pins, VPN-02 cross-check (default-port endpoints normalized via the shared `endpointOf`); optional `templates.path` registry (issue #15) loaded + validated fail-closed at config load (unknown server / non-allowed tool → `ConfigError`); optional admin **`write_allowlist`** (issue #16 / Q6) cross-checked against the manifest fail-closed — an entry naming an unknown server or a tool outside the server's `allowed_tools` is a `ConfigError` (the write allowlist is always a subset of the read allowlist, CP-02); an entry may carry per-arg `args` constraints (issue #16 v2); `policy.approval_ttl_seconds` (issue #16 v2, default 1800, capped at a day) — the default per-write approval window; `sandbox.enforce_memory_limit` (issue #18, default off) — the opt-in in-service hard memory bound on the bundled model |
| Sandbox Enforcer | `src/sandbox/` | runtime detection, real-path path confinement (symlink-escape refusal), capability manifest + reduced-mode report, NetworkGuard egress choke point |
| MCP Client Manager | `src/mcp/` | multi-server lifecycle (stdio/sse/http), startup validation, per-server tool allowlists (pre-wire), retries/backoff, terminal failures, args-by-hash audit; oauth/mtls fail closed (not yet implemented, refuse unauthenticated connects); all HTTP through NetworkGuard |
| File Manager | `src/files/` | confined CRUD, confirmation gates (incl. rename-onto-existing overwrite gate), undo journal (+subtree snapshots, re-resolved through confinement on undo, byte-exact binary restore), dry-run, ignore patterns (list checks against the confinement root), format validation (text/csv/json/md + the binary report formats docx/xlsx/pdf, magic-prefix validated); **byte-exact `writeBinary`** (issue #14) for the binary report artifacts — same confinement/ignore/confirmation gates + audit, journaled as base64 so undo is byte-exact; a text `write()` to a binary name is refused fail-closed; mutations + undo audited |
| Audit | `src/audit/` | append-only structured log (in-memory + JSONL, write failures surfaced), opt-in payloads, sink bridges, session transcript export |
| Orchestrator | `src/orchestrator/` | bounded fan-out, provenance claims, explicit gaps, report renderer (Markdown source of truth; HTML + **DOCX/XLSX/PDF** views over the same `(claims, gaps)`, issue #14 — text via `renderReport` / binary via `renderReportArtifact`, the artifact carried in-band as `reportArtifactB64` and written through the File Manager's `writeBinary`), progress events, **write-back (Q6, issue #16): `write-gate.ts` — the `WriteApprovalGate` contract + `ReadOnlyGate` (v1 default, refuses all writes) + `PolicyApprovalGate` (the admin write allowlist — with optional per-arg `args` constraints checked via ajv — AND a single-use, per-write, audited, **time-bound** `WriteApproval`; `decide()` resolves a task's recorded consent; `revoke()` withdraws it before use; refusal reasons `not-allowed-by-policy`/`args-not-allowed`/`no-approval`/`approval-expired`/`approval-revoked`/`gate-refused`, each a distinct audited denial); `Orchestrator.write()` audits every attempt and never retries a refusal; report-write failure is a surfaced `reportError`, claims/gaps never discarded; `request.ts` Zod request schema (shared CLI/plugin); `templates.ts` — recurring report templates (issue #15): the template registry schema + fail-closed `loadTemplateRegistry`, the shared `legalToolCatalog` + `validateRequest` (schema + legal tools, used by CLI/API/loop so nothing is legal because "saved"), `resolveTemplate`, `parseRunBody` |
| Sandy (composition) | `src/sandy.ts` | `createSandy(deps)` startup factory: config → enforcer → audit → engine → MCP → files → orchestrator; builds the write gate from config (a `PolicyApprovalGate` from `write_allowlist`, else the default `ReadOnlyGate` — fail closed); `check()` report (incl. engine health + the write posture: enabled + allowlist + approvalTtlSeconds), `run()`, `ask()`, `write(tasks, approvals)` (Q6), `close()` (reaps the model first). Injectable transport/detection for tests (no production-reachable override) |
| CLI | `src/cli.ts`, `bin/sandy.js` | `sandy check` + `sandy run <request.json|template>` (issue #15: an existing file is always the request file; otherwise the name is tried against the configured template registry — fail-closed, an unknown name is a usage error; a template run is audited `template_run`) + `sandy ask "<goal>"` (standalone: model plans → runs → narrates) + `sandy serve` (the long-lived standalone service: eager model start, loopback API, graceful shutdown); `--json`/`--no-progress`/`-c`/`-o`/`--port`; stable exit codes (0 ok / 1 error / 2 usage / 3 config / 4 sandbox) |
| Plugin (host tools) | `src/plugin/` | `tools.ts` (Zod tool-surface contract), `state.ts` (`PluginSession`/`SessionCache` — in-flight promise reserved synchronously, no double-build race —/`ProgressCollector`), `api.ts` (`SandyPluginAPI` — validate → delegate → shape, confirmation flow, structured errors, `sandy.write` approval flow), `mcp-server.ts` (MCP stdio server exposing 13 `sandy.*` tools incl. `sandy.model.usage` + `sandy.write` + the write-consent flow `sandy.write.approve` / `sandy.write.revoke` (issue #16 v2); `sandy.write` surfaces `needsApproval` for legal-but-unapproved tasks). `plugin/.claude-plugin/plugin.json` + `plugin/install.sh` (manual install, Q3) |
| LLM Engine | `src/engine.ts`, `src/memory-bound.ts` | The reasoning-layer seam (PRD §7) with a **lifecycle contract** (`start`/`isReady`/`status`/`close` + `record`/`invoke`). `HostLlmEngine` (plugin mode — the host is the engine, records token usage, never invokes); **`LlamaCppEngine`** (SD-02 local — `llama-server` subprocess on loopback, fail-closed on missing model, port discovery from the listen URL on **either** stream, child killed on a failed invocation, `--threads` budget from `sandbox.max_cpu_percent`, and the opt-in in-service hard memory bound (issue #18) — the model's pid is wrapped in a cgroup v2 child with `memory.max` = `sandbox.max_memory_mb`, fail-closed where there is no cgroup delegation); **`RemoteEngine`** (SD-04 — egress-guarded endpoint, bearer via `${ENV_REF}`); **`StubEngine`** (deterministic test/CI double, `fail()` for the dead-model path); `ModelRequest` has structured-output knobs (`responseFormat`/`jsonSchema`); `createLlmEngine` builds all four (fails closed on missing `model_path`/guard). The cgroup logic is in `src/memory-bound.ts` behind an injectable `MemoryBoundOps` (testable with no root) |
| Autonomous Loop | `src/standalone/loop.ts` | The Phase 2 reasoning loop (design §2.1): parse (bounded retry ≤3 with the error fed back, validated against `orchestratorRequestSchema` **and** the legal tool catalog via the shared `validateRequest` (issue #15) — "the model proposes, the schema disposes") → `Orchestrator.run` (unchanged) → optional narrate (clearly-labeled model summary re-rendered into the report; the prompt-injection threat model for narrate is documented in `docs/DECISIONS.md`). **Multi-turn / agentic planning (issue #19):** with `preferences.max_planning_rounds` > 1 (default 1 = the single pass), after each gather pass the model re-plans from the rounds gathered so far (`replan` — `stop` or additional gather tasks that pass the **same** `validatePlan()` gate as round 1; a replan that only re-proposes a call already made — `callSignature` — is "nothing-new", never re-gathered; on replan exhaustion the rounds gathered so far stand — `stop: "exhausted"`), and all rounds consolidate into ONE re-rendered report (claim refs renumbered, provenance untouched) with a narrate over the whole. Bounded (`max_planning_rounds` 1–5, fail-closed config), audited (`standalone_replan`), progress events `replan-*`. Deterministic conservative fallback (single named tool) or refuse-and-report with an explicit gap on exhaustion; never unbounded, never invents. Every model call audited (`model_invocation`), each step audited (`standalone_parse`/`standalone_plan`/`standalone_replan`/`standalone_narrate`); a dead model degrades, never crashes; `NoModelEngineError` fails closed against the host engine. Exposed as `Sandy.loop` / `sandy ask "<goal>"`; progress sink is swappable (`getProgressSink`/`setProgressSink`) so a service can redirect per-job progress in-band |
| Local API + Service | `src/standalone/api.ts` | The loopback-only REST API (SD-03, design §5) over the composed `Sandy`: plain `node:http` (no framework), binds `127.0.0.1` only (**off-loopback refused fail-closed**). `GET /health` → `check()`, `POST /run` (a raw request **or** `{ "template": "<name>" }`, issue #15) + `POST /ask` → `202`+id, `GET /jobs/:id` → status/result (incl. the `template` used), `GET /reports` (confined dir), `GET /audit` (transcript, AU-03), SSE `GET /jobs/:id/events` (Q4 progress). **Bounded** job store (`BoundedJobStore`: max pending → `429`, completed retention with oldest-evicted → clean `404`); a **serial** worker redirects the orchestrator's + loop's progress per job. **CSRF-hardened:** non-`application/json` Content-Type → `415`; foreign `Origin` → `403` (independent of CORS). The idle worker waits on an event-driven wake, not a busy-poll |
| Conformance | `conformance/` | `egress.test.ts` (in-process, incl. a model-present standalone leg) + `run-docker.sh`/`ep-server.mjs` (Docker network-level egress) + `sandbox-matrix.sh`/`signature.mjs`/`stdio-server.mjs` (Docker + Firejail runtime-agnosticity matrix, SB-10, + the opt-in `SANDY_REAL_MODEL` real-GGUF leg — issue #17) + `Dockerfile` (shared image) + `stub-model.mjs` (loopback stand-in for the bundled GGUF, design §9). **Both harnesses are parameterized** (`SANDY_MODE=standalone`), proving the no-egress / cross-sandbox guarantees hold for **both** modes (SD-05/06). CI: `.github/workflows/ci.yml` runs a `boundary × mode` (docker/firejail × plugin/standalone) matrix + byte-identical-signature identity check per mode |
| Model provisioning | `scripts/provision-model.sh`, `docs/MODEL.md` | Install-time, out-of-band: installs `llama-server` (Vulkan build) + the documented default model (Qwen3-4B-Instruct-2507 Q4_K_M, Apache-2.0), **both SHA256-pinned, fail-closed on mismatch** (the binary is verified before extraction; an override without an explicit hash fails closed), then prints the ready-to-paste `llm` block. The runtime itself never downloads a model |

Everything is exported from `src/index.ts`. Tests live in `tests/` (use `tests/helpers/mcp.ts` for in-process MCP servers).

## Remaining work (Phase 1 + Phase 2 complete — all §8 steps done)

Everything that was in the original remaining-work list (CLI, plugin, egress conformance, sandbox conformance, engine seam, backends, loop, API, service, standalone conformance, real-model end-to-end) is **done and released in v0.1.1** — the write-ups are in `docs/DIARY.md`. The deferred product scope (per `docs/DECISIONS.md` + PRD §10) and the optional hardening are **now done and released** — #14–#19 + the #16 approval-UX v2 half in v0.1.2, and the #14 binary report formats (DOCX/XLSX/PDF) in v0.1.3; the notes below are the record of what each covers and the genuinely-optional follow-ups that remain.

### Deferred product items

1. **Extra report formats — issue #14 — DONE (all five: Markdown, HTML, DOCX, XLSX, PDF).**
   - **Done (HTML, v0.1.2):** `renderReport(format, input)` + `renderHtmlReport` in `src/orchestrator/report.ts` — Markdown stays the source of truth, HTML is a deterministic view over the same `(claims, gaps)` (content identical, SD-06; all text HTML-escaped).
   - **Done (DOCX/XLSX/PDF, v0.1.3):** the three **binary** formats, each a deterministic view over the same `(claims, gaps)` (SD-06 — content identical across all five; only the presentation differs). They are **byte artifacts, not text**, so the string-based report pipeline gained a binary seam: `renderReportArtifact(format, input): Buffer` (all five) alongside the text-only `renderReport`; `OrchestratorResult`/`LoopResult`/`ReportToolResult` carry the artifact as `reportArtifactB64` (base64) in-band; the File Manager gained a byte-exact `writeBinary` (magic-prefix validated: ZIP `PK` for docx/xlsx, `%PDF-` for pdf; journaled as base64 so undo is byte-exact). The containers are **hand-rolled, zero-dependency** (the repo keeps the npm install clean): `src/orchestrator/zip.ts` (deterministic STORE ZIP — fixed timestamps + part order, so identical input → byte-identical archive), `docx.ts` (WordprocessingML), `xlsx.ts` (SpreadsheetML, one worksheet per section, title carried), `pdf.ts` (PDF 1.4, A4, base-14 Helvetica/WinAnsi, word-wrapped paginated flow, correct xref). Wired through the orchestrator's `writeBinaryReport`, `createOrchestrator`, the loop's multi-round **consolidation** + **narrate** re-renders, and the plugin's `sandy.report`. `REPORT_FORMATS` now lists all five, so the loader admits them; the format check stays fail-closed as defense in depth (schema vs `REPORT_FORMATS` divergence → `ConfigError`, never a silent Markdown fallback).
   - **Keep:** fail-closed (a corrupt/binary-named text write is refused, an unimplementable format is a config error); provenance/claims identical across formats (SD-06); zero new runtime dependencies.

2. **Recurring report templates (RG-08) — issue #15 — done (on-demand v1).**
   - **Done:** a template is exactly an `orchestratorRequestSchema` object (the same shape `sandy run <request.json>` and `POST /run` take). `src/orchestrator/templates.ts` holds the registry schema + fail-closed `loadTemplateRegistry`, the shared `legalToolCatalog` + `validateRequest` (the schema + legal tool catalog check the CLI, the API, and the loop's plan validation all now use — nothing new is legal because a request is "saved"), `resolveTemplate`, and `parseRunBody`. The registry is an optional `templates: { "path": "./templates.json" }` in `sandy.json` (example in `config/templates.json`), loaded and cross-checked against the manifest at config load (unknown server / non-allowed tool → `ConfigError`, fail-closed). `sandy run <template-name>` (an existing file always wins as a request file; an unknown name is a usage error) and `POST /run { "template": "<name>" }` (no registry → 400) both resolve to the unchanged `sandy.run`; a template run is audited (`template_run`) and the job carries its `template`.
   - **Remaining (v2, out of scope here):** "on a schedule" re-run — the supervisor's job (systemd/launchd/timer launching `sandy run <template>`; design §6), plus any template management UX.
   - **Keep:** a template is validated by the *same* `orchestratorRequestSchema` + legal tool catalog as an ad-hoc request (nothing new is legal because it's "saved"); fail-closed everywhere (bad registry, unknown template, illegal tool → refused, never a silent guess).

3. **Write-back (Q6 — the biggest, most design-heavy) — issue #16 — done (core + approval-UX v2 half).**
    - **Done (core, v1):** the gate contract got its non-default implementation. (a) **Admin write allowlist** — optional `write_allowlist` in `sandy.json` (issue #16 / Q6), cross-checked against the manifest fail-closed at config load: an entry naming an unknown server or a tool outside the server's `allowed_tools` is a `ConfigError`, so the write allowlist is always a *subset* of the read allowlist (CP-02: policy > preferences; the read allowlist is untouched). (b) **`PolicyApprovalGate`** (`src/orchestrator/write-gate.ts`) — a `WriteApprovalGate` that allows a write only when the target is on the allowlist AND an explicit `WriteApproval` (task-bound, single-use, named approver + reason) is presented; it is consumed on use and can never be replayed (no standing blanket consent). `createSandy` builds a `PolicyApprovalGate` when `write_allowlist` is configured, else the default **`ReadOnlyGate` (refuse all writes — fail closed)**. `Orchestrator.write(tasks, approvals)` routes an approved write to the server (still through the MCP tool allowlist + NetworkGuard), audits **every** attempt + decision (`write_attempt`, incl. the approver), surfaces progress events, and treats a refusal as terminal (never retried, never a claim/gap). `sandy.check` shows `write: { enabled, allowlist, approvalTtlSeconds }`.
    - **Done (v2, the "UX is the surface" half):** (a) **Per-arg constraints** — a `write_allowlist` entry may carry `args`, a JSON Schema fragment the write's args must satisfy (checked with ajv in the gate, the same library the MCP SDK uses for `structuredContent`): every stated constraint must hold and args may carry further keys; a constrained key must be present; a *malformed* constraint fails closed (`args-not-allowed`), never a silent no-op. (b) **Expiry + revocation** — approvals are time-bound by construction: `policy.approval_ttl_seconds` (default 1800, capped at a day) is the default window; an approval's explicit `expiresAt` may only shorten it; `PolicyApprovalGate.revoke()` withdraws consent before use (a revoked pair can never approve again); new audited refusal reasons `approval-expired` / `approval-revoked` (distinct from `no-approval`). (c) **Consent flow** — the plugin's **`sandy.write.approve`** (record the user's consent; the receipt carries the computed `expiresAt`) and **`sandy.write.revoke`** (withdraw it) tools; `decide()` resolves a task's pending approval, so inline `approvals` and recorded consent are symmetric; `sandy.write`'s result surfaces **`needsApproval`** (the `needsConfirmation` precedent for writes) listing the legal-but-unapproved tasks — a policy refusal is never listed (asking can't make it legal).
    - **Remaining (v3, if ever wanted):** a durable approval audit store (the in-session JSONL audit already records every `write_attempt` + decision) and any host-UI-specific consent rendering — both out of scope here.
    - **Keep:** never auto-confirm (FM-04 precedent); a write is a distinct audited event; the read allowlist is untouched; default = `ReadOnlyGate` (refuse all writes).

### Optional hardening (nice-to-have, not product scope)

- **`SANDY_REAL_MODEL` conformance leg — issue #17 — done (opt-in, CI stays green).** `conformance/sandbox-matrix.sh` gains an opt-in real-model leg: `SANDY_REAL_MODEL=<gguf>` runs `sandy check` + `sandy ask` against the real provisioned GGUF inside a no-egress Firejail jail (the shape of the manual proof, DIARY 2026-08-22 afternoon; write-up: DIARY 2026-08-25). Skipped, never failed, unless the env var is set + the file exists + `llama-server` is resolvable (overridable via `SANDY_LLM_SERVER`), so CI with no model is untouched; the Docker legs stay stub-based (no 2.4GB GGUF in the CI image). Asserts are on invariants that hold for any legal plan (model-sourced plan, `crm/read_deals`, provenance-tracked claim + `argsHash`, confined report, narrate audited, no orphan) — a real model proposes its own plan/args, so no canned-value asserts.
- **In-service hard memory bound — issue #18 — done (opt-in, fails closed).** The *default* ceiling is unchanged (the service manager's cgroup — `memory.max`/`--memory`/`MemoryMax=`). The flagged §4.5 addition is now implemented: `sandbox.enforce_memory_limit: true` (default **off**) wraps the model's process group in a cgroup v2 child whose `memory.max` is `sandbox.max_memory_mb` (`wrapProcessInMemoryCgroup` in `src/memory-bound.ts`, wired through `createSandy` → `createLlmEngine` → `LlamaCppEngine`, reported in `check().engine.memory`). It requires cgroup v2 **delegation** (a systemd scope/unit with `Delegate=yes`); in a boundary without it (the stock Docker conformance image, cgroup fs read-only) the engine **fails closed** (degraded) rather than run a model the operator asked to cap without the cap. All cgroup fs access is behind an injectable `MemoryBoundOps`, so the whole sequence (discovery / success / fail-closed / cleanup) is unit-tested with no root and no cgroupfs.
- **Multi-turn / agentic planning — issue #19 — done (bounded, opt-in, re-validated).** The design §10 v2 non-goal, now implemented: `preferences.max_planning_rounds` (default 1 = the single gather→report pass, so existing behavior is untouched) extends the validate-then-run seam in `src/standalone/loop.ts` — after each gather pass the model re-plans from the rounds gathered so far: `stop` (the data suffices) or ADDITIONAL gather tasks that pass the SAME `validatePlan()` gate as round 1 (the issue's invariant: schema + legal-tool-catalog validation applies to every round, not just the first). A replan that only re-proposes a call already made (`callSignature`: server + tool + canonicalized args) is "nothing-new" and ends the loop — no re-gathering churn. On replan exhaustion (a dead model / no legal follow-up) the rounds gathered so far stand (`stop: "exhausted"`) — reported, never a crash, never an invented plan. All rounds consolidate into ONE re-rendered report (claim refs renumbered; provenance untouched) and the narrate covers the whole. Bounded by the round cap (1–5, fail-closed config); audited (`standalone_replan`); progress events `replan-started`/`replan-attempt-failed`/`replan-stopped` (CLI/API/plugin sinks all render them).

## Conventions to keep
- **Fail closed** everywhere; never smooth over a gap; policy > preferences (tighten-never-loosen).
- Secrets only as `${ENV_REF}`; resolve at point of use, never store/log; args logged by hash only (AU-02).
- No network outside the declared MCP allowlist — everything goes through `NetworkGuard`.
- TypeScript strict, ESM, Node ≥ 22. **Pin `typescript@5.9`** (7.x native compiler has a `@types/node` auto-include bug — see DIARY 2026-08-17 afternoon).
- Run `npm run typecheck && npm test` after changes; `npm run build` for dist.
- Update `docs/DIARY.md` per work block; keep `README.md`'s Status section current. **Doc roles (per issue #13):** DIARY = chronological history (append-only), NEXT_STEPS = forward-looking state + this roadmap, README = short headline status + links. Don't restate a milestone in more than one of them.
- Commit style: conventional-commit prefixes (`feat:`, `fix:`, `docs:`, `test:`, `perf:`, `security:`, `release:`), imperative subject; after each completed task, commit + push to the remote (per `AGENTS.md`).

## Suggested first action for the next session

**Nothing is blocking, and the tracked deferred scope is now fully closed — including issue #14 in its entirety.** #14 (report formats — HTML in v0.1.2, **and the binary DOCX/XLSX/PDF renderers in v0.1.3**), #15 (on-demand report templates), #16 (the Q6 approval-gated write path — core **and** the approval-UX v2 half: consent flow, expiry/revocation, per-arg constraints), #17 (the `SANDY_REAL_MODEL` conformance leg), #18 (the in-service hard memory bound), and #19 (multi-turn / agentic planning) are all done. The only remaining items are the genuinely-optional follow-ups noted in the deferred list (scheduled template re-runs — the supervisor's job; a durable approval audit store). Read `docs/PHASE2_DESIGN.md` (fully implemented, §7 settled), `docs/MODEL.md` (provisioning + the default model), and the tail of `docs/DIARY.md` for the full write-ups.
